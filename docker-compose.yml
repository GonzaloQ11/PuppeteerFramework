version: '2'

services:
  tests:
    image: gonzalofquispe/puppeteerframework:latest
    command: "npm run test"
    volumes:
      - ./tests/:/tests/
      - ./utils/:/utils/
      - ./pages/:/pages/
